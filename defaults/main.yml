# SPDX-FileCopyrightText: 2023 Julian-Samuel Gebühr
# SPDX-FileCopyrightText: 2023 MASH project contributors
# SPDX-FileCopyrightText: 2023-2025 Slavi Pantaleev
# SPDX-FileCopyrightText: 2024 Nikita Chernyi
# SPDX-FileCopyrightText: 2025, 2026 Suguru Hirahara
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
# Project source code URL: https://github.com/karlomikus/mastodon/

mastodon_enabled: true

mastodon_identifier: mastodon
mastodon_base_path: "/{{ mastodon_identifier }}"
mastodon_data_path: "{{ mastodon_base_path }}/data"
mastodon_sidekiq_base_path: "{{ mastodon_base_path }}/sidekiq"
mastodon_streaming_base_path: "{{ mastodon_base_path }}/streaming"
mastodon_web_base_path: "{{ mastodon_base_path }}/web"

mastodon_uid: ""
mastodon_gid: ""

# The hostname at which Mastodon is served.
mastodon_hostname: ""

# The base container network. It will be auto-created by this role if it doesn't exist already.
mastodon_container_network: "{{ mastodon_identifier }}"

mastodon_database_hostname: ""
mastodon_database_port: 5432
mastodon_database_username: "mastodon"
mastodon_database_password: ""
mastodon_database_name: "mastodon"

mastodon_redis_hostname: ""
mastodon_redis_port: 6379

# Specify LOCAL_DOMAIN
# This identifies your server and cannot be changed safely later
mastodon_environment_variables_local_domain: ""

# Specify SECRET_KEY_BASE
# Use `bundle exec rails secret` to generate secrets
mastodon_environment_variables_secret_key_base: ""

# Specify secret values
# Do not share outside hosting environment!
# Use `bin/rails db:encryption:init` to generate fresh secrets
mastodon_environment_variables_active_record_encryption_deterministic_key: ""
mastodon_environment_variables_active_record_encryption_key_derivation_salt: ""
mastodon_environment_variables_active_record_encryption_primary_key: ""

# Specify VAPID_PRIVATE_KEY and VAPID_PUBLIC_KEY
# Use `bundle exec rails mastodon:webpush:generate_vapid_key` to generate ones
mastodon_environment_variables_vapid_private_key: ""
mastodon_environment_variables_vapid_public_key: ""

# Specify the mailer's configuration
mastodon_environment_variables_smtp_server: ""
mastodon_environment_variables_smtp_port: ""
mastodon_environment_variables_smtp_login: ""
mastodon_environment_variables_smtp_password: ""
mastodon_environment_variables_smtp_from_address: ""

# Specify the timezone
mastodon_environment_variables_tz: UTC

# Additional environment variables.
mastodon_environment_variables_additional_variables: ""

########################################################################
#                                                                      #
# sidekiq                                                              #
#                                                                      #
########################################################################

mastodon_sidekiq_identifier: "{{ mastodon_identifier }}-sidekiq"

# renovate: datasource=docker depName=ghcr.io/mastodon/mastodon
mastodon_sidekiq_version: v4.5.7

mastodon_sidekiq_scheme: https

mastodon_sidekiq_hostname: "{{ mastodon_hostname }}"

# The path at which the sidekiq is exposed
mastodon_sidekiq_path_prefix: "/"

mastodon_sidekiq_container_image: "{{ mastodon_sidekiq_container_image_registry_prefix }}mastodon/mastodon:{{ mastodon_sidekiq_container_image_tag }}"
mastodon_sidekiq_container_image_tag: "{{ mastodon_sidekiq_version }}"
mastodon_sidekiq_container_image_registry_prefix: "{{ mastodon_sidekiq_container_image_registry_prefix_upstream }}"
mastodon_sidekiq_container_image_registry_prefix_upstream: "{{ mastodon_sidekiq_container_image_registry_prefix_upstream_default }}"
mastodon_sidekiq_container_image_registry_prefix_upstream_default: ghcr.io/
mastodon_sidekiq_container_image_force_pull: "{{ mastodon_sidekiq_container_image.endswith(':latest') }}"

mastodon_sidekiq_container_network: "{{ mastodon_container_network }}"

# A list of additional container networks that the container would be connected to.
# The role does not create these networks, so make sure they already exist.
# Use this to expose this container to another reverse proxy, which runs in a different container network.
mastodon_sidekiq_container_additional_networks: "{{ mastodon_sidekiq_container_additional_networks_auto + mastodon_sidekiq_container_additional_networks_custom }}"
mastodon_sidekiq_container_additional_networks_auto: []
mastodon_sidekiq_container_additional_networks_custom: []

# A list of additional "volumes" to mount in the container.
# This list gets populated dynamically at runtime. You can provide a different default value,
# if you wish to mount your own files into the container.
# Contains definition objects like this: `{"type": "bind", "src": "/outside", "dst": "/inside", "options": "readonly"}.
# See the `--mount` documentation for the `docker run` command.
mastodon_sidekiq_container_additional_volumes: "{{ mastodon_sidekiq_container_additional_volumes_auto + mastodon_sidekiq_container_additional_volumes_custom }}"
mastodon_sidekiq_container_additional_volumes_auto: []
mastodon_sidekiq_container_additional_volumes_custom: []

# mastodon_sidekiq_container_labels_traefik_enabled controls whether labels to assist a Traefik reverse-proxy will be attached to the container.
# See `../templates/labels_sidekiq.j2` for details.
#
# To inject your own other container labels, see `mastodon_sidekiq_container_labels_additional_labels`.
mastodon_sidekiq_container_labels_traefik_enabled: true
mastodon_sidekiq_container_labels_traefik_docker_network: "{{ mastodon_sidekiq_container_network }}"
mastodon_sidekiq_container_labels_traefik_hostname: "{{ mastodon_sidekiq_hostname }}"
# The path prefix must either be `/` or not end with a slash (e.g. `/sidekiq`).
mastodon_sidekiq_container_labels_traefik_path_prefix: "{{ mastodon_sidekiq_path_prefix }}"
mastodon_sidekiq_container_labels_traefik_rule: "Host(`{{ mastodon_sidekiq_container_labels_traefik_hostname }}`){% if mastodon_sidekiq_container_labels_traefik_path_prefix != '/' %} && PathPrefix(`{{ mastodon_sidekiq_container_labels_traefik_path_prefix }}`){% endif %}"
mastodon_sidekiq_container_labels_traefik_priority: 0
mastodon_sidekiq_container_labels_traefik_entrypoints: web-secure
mastodon_sidekiq_container_labels_traefik_tls: "{{ mastodon_sidekiq_container_labels_traefik_entrypoints != 'web' }}"
mastodon_sidekiq_container_labels_traefik_tls_certResolver: default  # noqa var-naming

# Controls which additional headers to attach to all HTTP requests.
# To add your own custom request headers, use `mastodon_sidekiq_container_labels_traefik_additional_request_headers_custom`
mastodon_sidekiq_container_labels_traefik_additional_request_headers: "{{ mastodon_sidekiq_container_labels_traefik_additional_request_headers_auto | combine(mastodon_sidekiq_container_labels_traefik_additional_request_headers_custom) }}"
mastodon_sidekiq_container_labels_traefik_additional_request_headers_auto: {}
mastodon_sidekiq_container_labels_traefik_additional_request_headers_custom: {}

# Controls which additional headers to attach to all HTTP responses.
# To add your own custom response headers, use `mastodon_sidekiq_container_labels_traefik_additional_response_headers_custom`
mastodon_sidekiq_container_labels_traefik_additional_response_headers: "{{ mastodon_sidekiq_container_labels_traefik_additional_response_headers_auto | combine(mastodon_sidekiq_container_labels_traefik_additional_response_headers_custom) }}"
mastodon_sidekiq_container_labels_traefik_additional_response_headers_auto: |
  {{
    {}
    | combine ({'X-XSS-Protection': mastodon_sidekiq_http_header_xss_protection} if mastodon_sidekiq_http_header_xss_protection else {})
    | combine ({'X-Content-Type-Options': mastodon_sidekiq_http_header_content_type_options} if mastodon_sidekiq_http_header_content_type_options else {})
    | combine ({'Content-Security-Policy': mastodon_sidekiq_http_header_content_security_policy} if mastodon_sidekiq_http_header_content_security_policy else {})
    | combine ({'Permissions-Policy': mastodon_sidekiq_http_header_permissions_policy} if mastodon_sidekiq_http_header_permissions_policy else {})
    | combine ({'Strict-Transport-Security': mastodon_sidekiq_http_header_strict_transport_security} if mastodon_sidekiq_http_header_strict_transport_security and mastodon_sidekiq_container_labels_traefik_tls else {})
  }}
mastodon_sidekiq_container_labels_traefik_additional_response_headers_custom: {}

# mastodon_sidekiq_container_labels_additional_labels contains a multiline string with additional labels to add to the container label file.
# See `../templates/labels_sidekiq.j2` for details.
# To add your own custom labels, use `mastodon_sidekiq_container_labels_additional_labels_custom`
#
# Example:
# mastodon_sidekiq_container_labels_additional_labels_custom:
#   - my.label=1
#   - another.label="here"
mastodon_sidekiq_container_labels_additional_labels: "{{ mastodon_sidekiq_container_labels_additional_labels_auto + mastodon_sidekiq_container_labels_additional_labels_custom }}"
mastodon_sidekiq_container_labels_additional_labels_auto: []
mastodon_sidekiq_container_labels_additional_labels_custom: []

# A list of extra arguments to pass to the container (`docker create` command)
mastodon_sidekiq_container_extra_arguments: "{{ mastodon_sidekiq_container_extra_arguments_auto + mastodon_sidekiq_container_extra_arguments_custom }}"
mastodon_sidekiq_container_extra_arguments_auto: []
mastodon_sidekiq_container_extra_arguments_custom: []

# Specifies the value of the `X-XSS-Protection` header
# Stops pages from loading when they detect reflected cross-site scripting (XSS) attacks.
#
# Learn more about it is here:
# - https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/X-XSS-Protection
# - https://portswigger.net/web-security/cross-site-scripting/reflected
mastodon_sidekiq_http_header_xss_protection: "1; mode=block"

# Specifies the value of the `X-Content-Type-Options` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/X-Content-Type-Options
mastodon_sidekiq_http_header_content_type_options: nosniff

# Specifies the value of the `Content-Security-Policy` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Content-Security-Policy
mastodon_sidekiq_http_header_content_security_policy: frame-ancestors 'self'

# Specifies the value of the `Permissions-Policy` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Permissions-Policy
mastodon_sidekiq_http_header_permissions_policy: "{{ 'interest-cohort=()' if mastodon_sidekiq_floc_optout_enabled else '' }}"

# Specifies the value of the `Strict-Transport-Security` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Strict-Transport-Security
mastodon_sidekiq_http_header_strict_transport_security: "max-age=31536000; includeSubDomains{{ '; preload' if mastodon_sidekiq_hsts_preload_enabled else '' }}"

# Controls whether to send a "Permissions-Policy interest-cohort=();" header along with all responses
#
# Learn more about what it is here:
# - https://www.eff.org/deeplinks/2021/03/googles-floc-terrible-idea
# - https://paramdeo.com/blog/opting-your-website-out-of-googles-floc-network
# - https://amifloced.org/
#
# Of course, a better solution is to just stop using browsers (like Chrome), which participate in such tracking practices.
# See: `mastodon_sidekiq_http_header_permissions_policy`
mastodon_sidekiq_floc_optout_enabled: true

# Controls if HSTS preloading is enabled
#
# In its strongest and recommended form, the [HSTS policy](https://www.chromium.org/hsts) includes all subdomains, and
# indicates a willingness to be "preloaded" into browsers:
# `Strict-Transport-Security: max-age=31536000; includeSubDomains; preload`
# For more information visit:
# - https://en.wikipedia.org/wiki/HTTP_Strict_Transport_Security
# - https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Strict-Transport-Security
# - https://hstspreload.org/#opt-in
# See: `mastodon_http_header_strict_transport_security`
mastodon_sidekiq_hsts_preload_enabled: false

# List of systemd services that the Mastodon sidekiq systemd service depends on
mastodon_sidekiq_systemd_required_services_list: "{{ mastodon_sidekiq_systemd_required_services_list_default + mastodon_sidekiq_systemd_required_services_list_auto + mastodon_sidekiq_systemd_required_services_list_custom }}"
mastodon_sidekiq_systemd_required_services_list_default: "{{ [devture_systemd_docker_base_docker_service_name] if devture_systemd_docker_base_docker_service_name else [] }}"
mastodon_sidekiq_systemd_required_services_list_auto: []
mastodon_sidekiq_systemd_required_services_list_custom: []

# List of systemd services that the Mastodon sidekiq systemd service wants
mastodon_sidekiq_systemd_wanted_services_list: "{{ mastodon_sidekiq_systemd_wanted_services_list_default + mastodon_sidekiq_systemd_wanted_services_list_auto + mastodon_sidekiq_systemd_wanted_services_list_custom }}"
mastodon_sidekiq_systemd_wanted_services_list_default: []
mastodon_sidekiq_systemd_wanted_services_list_auto: []
mastodon_sidekiq_systemd_wanted_services_list_custom: []

########################################################################
#                                                                      #
# /sidekiq                                                             #
#                                                                      #
########################################################################

########################################################################
#                                                                      #
# streaming                                                            #
#                                                                      #
########################################################################

mastodon_streaming_identifier: "{{ mastodon_identifier }}-streaming"

# renovate: datasource=docker depName=ghcr.io/mastodon/mastodon-streaming
mastodon_streaming_version: v4.5.7

mastodon_streaming_scheme: https

mastodon_streaming_hostname: "{{ mastodon_hostname }}"

# The path at which streaming is exposed
mastodon_streaming_path_prefix: "/"

mastodon_streaming_container_image: "{{ mastodon_streaming_container_image_registry_prefix }}mastodon/mastodon-streaming:{{ mastodon_streaming_container_image_tag }}"
mastodon_streaming_container_image_tag: "{{ mastodon_streaming_version }}"
mastodon_streaming_container_image_registry_prefix: "{{ mastodon_streaming_container_image_registry_prefix_upstream }}"
mastodon_streaming_container_image_registry_prefix_upstream: "{{ mastodon_streaming_container_image_registry_prefix_upstream_default }}"
mastodon_streaming_container_image_registry_prefix_upstream_default: ghcr.io/
mastodon_streaming_container_image_force_pull: "{{ mastodon_streaming_container_image.endswith(':latest') }}"

# Controls whether the Mastodon streaming container exposes its HTTP port (as defined by `mastodon_streaming_container_http_port`).
#
# Takes an "<ip>:<port>" or "<port>" value (e.g. "127.0.0.1:3001"), or empty string to not expose.
mastodon_streaming_container_http_host_bind_port: ""

mastodon_streaming_container_network: "{{ mastodon_container_network }}"

# The port number in the container
mastodon_streaming_container_http_port: 4000

# A list of additional container networks that the container would be connected to.
# The role does not create these networks, so make sure they already exist.
# Use this to expose this container to another reverse proxy, which runs in a different container network.
mastodon_streaming_container_additional_networks: "{{ mastodon_streaming_container_additional_networks_auto + mastodon_streaming_container_additional_networks_custom }}"
mastodon_streaming_container_additional_networks_auto: []
mastodon_streaming_container_additional_networks_custom: []

# A list of additional "volumes" to mount in the container.
# This list gets populated dynamically at runtime. You can provide a different default value,
# if you wish to mount your own files into the container.
# Contains definition objects like this: `{"type": "bind", "src": "/outside", "dst": "/inside", "options": "readonly"}.
# See the `--mount` documentation for the `docker run` command.
mastodon_streaming_container_additional_volumes: "{{ mastodon_streaming_container_additional_volumes_auto + mastodon_streaming_container_additional_volumes_custom }}"
mastodon_streaming_container_additional_volumes_auto: []
mastodon_streaming_container_additional_volumes_custom: []

# mastodon_streaming_container_labels_traefik_enabled controls whether labels to assist a Traefik reverse-proxy will be attached to the container.
# See `../templates/labels_streaming.j2` for details.
#
# To inject your own other container labels, see `mastodon_streaming_container_labels_additional_labels`.
mastodon_streaming_container_labels_traefik_enabled: true
mastodon_streaming_container_labels_traefik_docker_network: "{{ mastodon_streaming_container_network }}"
mastodon_streaming_container_labels_traefik_hostname: "{{ mastodon_streaming_hostname }}"
# The path prefix must either be `/` or not end with a slash (e.g. `/streaming`).
mastodon_streaming_container_labels_traefik_path_prefix: "{{ mastodon_streaming_path_prefix }}"
mastodon_streaming_container_labels_traefik_rule: "Host(`{{ mastodon_streaming_container_labels_traefik_hostname }}`){% if mastodon_streaming_container_labels_traefik_path_prefix != '/' %} && PathPrefix(`{{ mastodon_streaming_container_labels_traefik_path_prefix }}`){% endif %}"
mastodon_streaming_container_labels_traefik_priority: 0
mastodon_streaming_container_labels_traefik_entrypoints: web-secure
mastodon_streaming_container_labels_traefik_tls: "{{ mastodon_streaming_container_labels_traefik_entrypoints != 'web' }}"
mastodon_streaming_container_labels_traefik_tls_certResolver: default  # noqa var-naming

# Controls which additional headers to attach to all HTTP requests.
# To add your own custom request headers, use `mastodon_streaming_container_labels_traefik_additional_request_headers_custom`
mastodon_streaming_container_labels_traefik_additional_request_headers: "{{ mastodon_streaming_container_labels_traefik_additional_request_headers_auto | combine(mastodon_streaming_container_labels_traefik_additional_request_headers_custom) }}"
mastodon_streaming_container_labels_traefik_additional_request_headers_auto: {}
mastodon_streaming_container_labels_traefik_additional_request_headers_custom: {}

# Controls which additional headers to attach to all HTTP responses.
# To add your own custom response headers, use `mastodon_streaming_container_labels_traefik_additional_response_headers_custom`
mastodon_streaming_container_labels_traefik_additional_response_headers: "{{ mastodon_streaming_container_labels_traefik_additional_response_headers_auto | combine(mastodon_streaming_container_labels_traefik_additional_response_headers_custom) }}"
mastodon_streaming_container_labels_traefik_additional_response_headers_auto: |
  {{
    {}
    | combine ({'X-XSS-Protection': mastodon_streaming_http_header_xss_protection} if mastodon_streaming_http_header_xss_protection else {})
    | combine ({'X-Content-Type-Options': mastodon_streaming_http_header_content_type_options} if mastodon_streaming_http_header_content_type_options else {})
    | combine ({'Content-Security-Policy': mastodon_streaming_http_header_content_security_policy} if mastodon_streaming_http_header_content_security_policy else {})
    | combine ({'Permissions-Policy': mastodon_streaming_http_header_permissions_policy} if mastodon_streaming_http_header_permissions_policy else {})
    | combine ({'Strict-Transport-Security': mastodon_streaming_http_header_strict_transport_security} if mastodon_streaming_http_header_strict_transport_security and mastodon_streaming_container_labels_traefik_tls else {})
  }}
mastodon_streaming_container_labels_traefik_additional_response_headers_custom: {}

# mastodon_streaming_container_labels_additional_labels contains a multiline string with additional labels to add to the container label file.
# See `../templates/labels_streaming.j2` for details.
# To add your own custom labels, use `mastodon_streaming_container_labels_additional_labels_custom`
#
# Example:
# mastodon_streaming_container_labels_additional_labels_custom:
#   - my.label=1
#   - another.label="here"
mastodon_streaming_container_labels_additional_labels: "{{ mastodon_streaming_container_labels_additional_labels_auto + mastodon_streaming_container_labels_additional_labels_custom }}"
mastodon_streaming_container_labels_additional_labels_auto: []
mastodon_streaming_container_labels_additional_labels_custom: []

# A list of extra arguments to pass to the container (`docker create` command)
mastodon_streaming_container_extra_arguments: "{{ mastodon_streaming_container_extra_arguments_auto + mastodon_streaming_container_extra_arguments_custom }}"
mastodon_streaming_container_extra_arguments_auto: []
mastodon_streaming_container_extra_arguments_custom: []

# Specifies the value of the `X-XSS-Protection` header
# Stops pages from loading when they detect reflected cross-site scripting (XSS) attacks.
#
# Learn more about it is here:
# - https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/X-XSS-Protection
# - https://portswigger.net/web-security/cross-site-scripting/reflected
mastodon_streaming_http_header_xss_protection: "1; mode=block"

# Specifies the value of the `X-Content-Type-Options` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/X-Content-Type-Options
mastodon_streaming_http_header_content_type_options: nosniff

# Specifies the value of the `Content-Security-Policy` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Content-Security-Policy
mastodon_streaming_http_header_content_security_policy: frame-ancestors 'self'

# Specifies the value of the `Permissions-Policy` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Permissions-Policy
mastodon_streaming_http_header_permissions_policy: "{{ 'interest-cohort=()' if mastodon_streaming_floc_optout_enabled else '' }}"

# Specifies the value of the `Strict-Transport-Security` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Strict-Transport-Security
mastodon_streaming_http_header_strict_transport_security: "max-age=31536000; includeSubDomains{{ '; preload' if mastodon_streaming_hsts_preload_enabled else '' }}"

# Controls whether to send a "Permissions-Policy interest-cohort=();" header along with all responses
#
# Learn more about what it is here:
# - https://www.eff.org/deeplinks/2021/03/googles-floc-terrible-idea
# - https://paramdeo.com/blog/opting-your-website-out-of-googles-floc-network
# - https://amifloced.org/
#
# Of course, a better solution is to just stop using browsers (like Chrome), which participate in such tracking practices.
# See: `mastodon_streaming_http_header_permissions_policy`
mastodon_streaming_floc_optout_enabled: true

# Controls if HSTS preloading is enabled
#
# In its strongest and recommended form, the [HSTS policy](https://www.chromium.org/hsts) includes all subdomains, and
# indicates a willingness to be "preloaded" into browsers:
# `Strict-Transport-Security: max-age=31536000; includeSubDomains; preload`
# For more information visit:
# - https://en.wikipedia.org/wiki/HTTP_Strict_Transport_Security
# - https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Strict-Transport-Security
# - https://hstspreload.org/#opt-in
# See: `mastodon_http_header_strict_transport_security`
mastodon_streaming_hsts_preload_enabled: false

# List of systemd services that the Mastodon streaming systemd service depends on
mastodon_streaming_systemd_required_services_list: "{{ mastodon_streaming_systemd_required_services_list_default + mastodon_streaming_systemd_required_services_list_auto + mastodon_streaming_systemd_required_services_list_custom }}"
mastodon_streaming_systemd_required_services_list_default: "{{ [devture_systemd_docker_base_docker_service_name] if devture_systemd_docker_base_docker_service_name else [] }}"
mastodon_streaming_systemd_required_services_list_auto: []
mastodon_streaming_systemd_required_services_list_custom: []

# List of systemd services that the Mastodon streaming systemd service wants
mastodon_streaming_systemd_wanted_services_list: "{{ mastodon_streaming_systemd_wanted_services_list_default + mastodon_streaming_systemd_wanted_services_list_auto + mastodon_streaming_systemd_wanted_services_list_custom }}"
mastodon_streaming_systemd_wanted_services_list_default: []
mastodon_streaming_systemd_wanted_services_list_auto: []
mastodon_streaming_systemd_wanted_services_list_custom: []

########################################################################
#                                                                      #
# /streaming                                                           #
#                                                                      #
########################################################################

########################################################################
#                                                                      #
# web                                                                  #
#                                                                      #
########################################################################

mastodon_web_identifier: "{{ mastodon_identifier }}-web"

# renovate: datasource=docker depName=ghcr.io/mastodon/mastodon
mastodon_web_version: v4.5.7

mastodon_web_scheme: https

mastodon_web_hostname: "{{ mastodon_hostname }}"

# The path at which web is exposed
mastodon_web_path_prefix: "/"

mastodon_web_container_image: "{{ mastodon_web_container_image_registry_prefix }}mastodon/mastodon:{{ mastodon_web_container_image_tag }}"
mastodon_web_container_image_tag: "{{ mastodon_web_version }}"
mastodon_web_container_image_registry_prefix: "{{ mastodon_web_container_image_registry_prefix_upstream }}"
mastodon_web_container_image_registry_prefix_upstream: "{{ mastodon_web_container_image_registry_prefix_upstream_default }}"
mastodon_web_container_image_registry_prefix_upstream_default: ghcr.io/
mastodon_web_container_image_force_pull: "{{ mastodon_web_container_image.endswith(':latest') }}"

# Controls whether the Mastodon web container exposes its HTTP port (as defined by `mastodon_web_container_http_port`).
#
# Takes an "<ip>:<port>" or "<port>" value (e.g. "127.0.0.1:3001"), or empty string to not expose.
mastodon_web_container_http_host_bind_port: ""

mastodon_web_container_network: "{{ mastodon_container_network }}"

# The port number in the container
mastodon_web_container_http_port: 3000

# A list of additional container networks that the container would be connected to.
# The role does not create these networks, so make sure they already exist.
# Use this to expose this container to another reverse proxy, which runs in a different container network.
mastodon_web_container_additional_networks: "{{ mastodon_web_container_additional_networks_auto + mastodon_web_container_additional_networks_custom }}"
mastodon_web_container_additional_networks_auto: []
mastodon_web_container_additional_networks_custom: []

# A list of additional "volumes" to mount in the container.
# This list gets populated dynamically at runtime. You can provide a different default value,
# if you wish to mount your own files into the container.
# Contains definition objects like this: `{"type": "bind", "src": "/outside", "dst": "/inside", "options": "readonly"}.
# See the `--mount` documentation for the `docker run` command.
mastodon_web_container_additional_volumes: "{{ mastodon_web_container_additional_volumes_auto + mastodon_web_container_additional_volumes_custom }}"
mastodon_web_container_additional_volumes_auto: []
mastodon_web_container_additional_volumes_custom: []

# mastodon_web_container_labels_traefik_enabled controls whether labels to assist a Traefik reverse-proxy will be attached to the container.
# See `../templates/labels_web.j2` for details.
#
# To inject your own other container labels, see `mastodon_web_container_labels_additional_labels`.
mastodon_web_container_labels_traefik_enabled: true
mastodon_web_container_labels_traefik_docker_network: "{{ mastodon_web_container_network }}"
mastodon_web_container_labels_traefik_hostname: "{{ mastodon_web_hostname }}"
# The path prefix must either be `/` or not end with a slash (e.g. `/web`).
mastodon_web_container_labels_traefik_path_prefix: "{{ mastodon_web_path_prefix }}"
mastodon_web_container_labels_traefik_rule: "Host(`{{ mastodon_web_container_labels_traefik_hostname }}`){% if mastodon_web_container_labels_traefik_path_prefix != '/' %} && PathPrefix(`{{ mastodon_web_container_labels_traefik_path_prefix }}`){% endif %}"
mastodon_web_container_labels_traefik_priority: 0
mastodon_web_container_labels_traefik_entrypoints: web-secure
mastodon_web_container_labels_traefik_tls: "{{ mastodon_web_container_labels_traefik_entrypoints != 'web' }}"
mastodon_web_container_labels_traefik_tls_certResolver: default  # noqa var-naming

# Controls which additional headers to attach to all HTTP requests.
# To add your own custom request headers, use `mastodon_web_container_labels_traefik_additional_request_headers_custom`
mastodon_web_container_labels_traefik_additional_request_headers: "{{ mastodon_web_container_labels_traefik_additional_request_headers_auto | combine(mastodon_web_container_labels_traefik_additional_request_headers_custom) }}"
mastodon_web_container_labels_traefik_additional_request_headers_auto: {}
mastodon_web_container_labels_traefik_additional_request_headers_custom: {}

# Controls which additional headers to attach to all HTTP responses.
# To add your own custom response headers, use `mastodon_web_container_labels_traefik_additional_response_headers_custom`
mastodon_web_container_labels_traefik_additional_response_headers: "{{ mastodon_web_container_labels_traefik_additional_response_headers_auto | combine(mastodon_web_container_labels_traefik_additional_response_headers_custom) }}"
mastodon_web_container_labels_traefik_additional_response_headers_auto: |
  {{
    {}
    | combine ({'X-XSS-Protection': mastodon_web_http_header_xss_protection} if mastodon_web_http_header_xss_protection else {})
    | combine ({'X-Content-Type-Options': mastodon_web_http_header_content_type_options} if mastodon_web_http_header_content_type_options else {})
    | combine ({'Content-Security-Policy': mastodon_web_http_header_content_security_policy} if mastodon_web_http_header_content_security_policy else {})
    | combine ({'Permissions-Policy': mastodon_web_http_header_permissions_policy} if mastodon_web_http_header_permissions_policy else {})
    | combine ({'Strict-Transport-Security': mastodon_web_http_header_strict_transport_security} if mastodon_web_http_header_strict_transport_security and mastodon_web_container_labels_traefik_tls else {})
  }}
mastodon_web_container_labels_traefik_additional_response_headers_custom: {}

# mastodon_web_container_labels_additional_labels contains a multiline string with additional labels to add to the container label file.
# See `../templates/labels_web.j2` for details.
# To add your own custom labels, use `mastodon_web_container_labels_additional_labels_custom`
#
# Example:
# mastodon_web_container_labels_additional_labels_custom:
#   - my.label=1
#   - another.label="here"
mastodon_web_container_labels_additional_labels: "{{ mastodon_web_container_labels_additional_labels_auto + mastodon_web_container_labels_additional_labels_custom }}"
mastodon_web_container_labels_additional_labels_auto: []
mastodon_web_container_labels_additional_labels_custom: []

# A list of extra arguments to pass to the container (`docker create` command)
mastodon_web_container_extra_arguments: "{{ mastodon_web_container_extra_arguments_auto + mastodon_web_container_extra_arguments_custom }}"
mastodon_web_container_extra_arguments_auto: []
mastodon_web_container_extra_arguments_custom: []

# Specifies the value of the `X-XSS-Protection` header
# Stops pages from loading when they detect reflected cross-site scripting (XSS) attacks.
#
# Learn more about it is here:
# - https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/X-XSS-Protection
# - https://portswigger.net/web-security/cross-site-scripting/reflected
mastodon_web_http_header_xss_protection: "1; mode=block"

# Specifies the value of the `X-Content-Type-Options` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/X-Content-Type-Options
mastodon_web_http_header_content_type_options: nosniff

# Specifies the value of the `Content-Security-Policy` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Content-Security-Policy
mastodon_web_http_header_content_security_policy: frame-ancestors 'self'

# Specifies the value of the `Permissions-Policy` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Permissions-Policy
mastodon_web_http_header_permissions_policy: "{{ 'interest-cohort=()' if mastodon_web_floc_optout_enabled else '' }}"

# Specifies the value of the `Strict-Transport-Security` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Strict-Transport-Security
mastodon_web_http_header_strict_transport_security: "max-age=31536000; includeSubDomains{{ '; preload' if mastodon_web_hsts_preload_enabled else '' }}"

# Controls whether to send a "Permissions-Policy interest-cohort=();" header along with all responses
#
# Learn more about what it is here:
# - https://www.eff.org/deeplinks/2021/03/googles-floc-terrible-idea
# - https://paramdeo.com/blog/opting-your-website-out-of-googles-floc-network
# - https://amifloced.org/
#
# Of course, a better solution is to just stop using browsers (like Chrome), which participate in such tracking practices.
# See: `mastodon_web_http_header_permissions_policy`
mastodon_web_floc_optout_enabled: true

# Controls if HSTS preloading is enabled
#
# In its strongest and recommended form, the [HSTS policy](https://www.chromium.org/hsts) includes all subdomains, and
# indicates a willingness to be "preloaded" into browsers:
# `Strict-Transport-Security: max-age=31536000; includeSubDomains; preload`
# For more information visit:
# - https://en.wikipedia.org/wiki/HTTP_Strict_Transport_Security
# - https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Strict-Transport-Security
# - https://hstspreload.org/#opt-in
# See: `mastodon_http_header_strict_transport_security`
mastodon_web_hsts_preload_enabled: false

# List of systemd services that the Mastodon web systemd service depends on
mastodon_web_systemd_required_services_list: "{{ mastodon_web_systemd_required_services_list_default + mastodon_web_systemd_required_services_list_auto + mastodon_web_systemd_required_services_list_custom }}"
mastodon_web_systemd_required_services_list_default: "{{ [devture_systemd_docker_base_docker_service_name] if devture_systemd_docker_base_docker_service_name else [] }}"
mastodon_web_systemd_required_services_list_auto: []
mastodon_web_systemd_required_services_list_custom: []

# List of systemd services that the Mastodon web systemd service wants
mastodon_web_systemd_wanted_services_list: "{{ mastodon_web_systemd_wanted_services_list_default + mastodon_web_systemd_wanted_services_list_auto + mastodon_web_systemd_wanted_services_list_custom }}"
mastodon_web_systemd_wanted_services_list_default: []
mastodon_web_systemd_wanted_services_list_auto: []
mastodon_web_systemd_wanted_services_list_custom: []

mastodon_web_migration_enabled: false

########################################################################
#                                                                      #
# /web                                                                 #
#                                                                      #
########################################################################
